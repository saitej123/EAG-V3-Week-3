/**
 * Default Gemini models for FairFrame (Google AI Studio / Gemini API).
 * Preview IDs change — see https://ai.google.dev/gemini-api/docs/models
 */
export const DEFAULT_GEMINI_AUDIT_MODEL = "gemini-3-flash-preview";

/** Native image output (mockups / annotated wireframes). Uses responseModalities TEXT + IMAGE. */
export const DEFAULT_GEMINI_IMAGE_MODEL = "gemini-3.1-flash-image-preview";
